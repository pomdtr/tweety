{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            // "preLaunchTask": "build frontend",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": []
        }
    ],
}
